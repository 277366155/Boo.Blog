version: '3.7'

services:
  grpcclient:
    image: grpcservice
    hostname: grpcService
    build: .
    container_name: 'grpcService'
    restart: always
    extra_hosts:
        ljtest.td365.com.cn: 128.0.23.63